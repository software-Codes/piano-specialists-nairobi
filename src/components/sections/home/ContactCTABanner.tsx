import { SectionHeader } from "@/components/shared/SectionHeader";

export function ContactCTABanner() {
  return (
    <section className="section-space border-t border-border bg-muted/30">
      <div className="page-shell">
        <SectionHeader
          eyebrow="Contact"
          title="Contact CTA scaffold"
          description="Placeholder section for a closing homepage call to action."
        />
      </div>
    </section>
  );
}
